version: 2.1

orbs:
  ruby: circleci/ruby@1.2.0

jobs:
  eloqua-api:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - ruby/install:
          version: '2.6.3'
      - ruby/install-deps:
          key: '1:11'
          bundler-version: '1.17'
workflows:
  example-workflow:
    jobs:
      - eloqua-api