RSpec.describe Eloqua::Api::Wrapper do
  it "has a version number" do
    expect(Eloqua::Api::Wrapper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
